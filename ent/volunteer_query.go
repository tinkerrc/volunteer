// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/tinkerrc/volunteer/ent/eventvolunteer"
	"github.com/tinkerrc/volunteer/ent/predicate"
	"github.com/tinkerrc/volunteer/ent/training"
	"github.com/tinkerrc/volunteer/ent/volunteer"
)

// VolunteerQuery is the builder for querying Volunteer entities.
type VolunteerQuery struct {
	config
	ctx                  *QueryContext
	order                []volunteer.OrderOption
	inters               []Interceptor
	predicates           []predicate.Volunteer
	withVolunteerRecords *EventVolunteerQuery
	withTrainings        *TrainingQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the VolunteerQuery builder.
func (vq *VolunteerQuery) Where(ps ...predicate.Volunteer) *VolunteerQuery {
	vq.predicates = append(vq.predicates, ps...)
	return vq
}

// Limit the number of records to be returned by this query.
func (vq *VolunteerQuery) Limit(limit int) *VolunteerQuery {
	vq.ctx.Limit = &limit
	return vq
}

// Offset to start from.
func (vq *VolunteerQuery) Offset(offset int) *VolunteerQuery {
	vq.ctx.Offset = &offset
	return vq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (vq *VolunteerQuery) Unique(unique bool) *VolunteerQuery {
	vq.ctx.Unique = &unique
	return vq
}

// Order specifies how the records should be ordered.
func (vq *VolunteerQuery) Order(o ...volunteer.OrderOption) *VolunteerQuery {
	vq.order = append(vq.order, o...)
	return vq
}

// QueryVolunteerRecords chains the current query on the "volunteer_records" edge.
func (vq *VolunteerQuery) QueryVolunteerRecords() *EventVolunteerQuery {
	query := (&EventVolunteerClient{config: vq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := vq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := vq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(volunteer.Table, volunteer.FieldID, selector),
			sqlgraph.To(eventvolunteer.Table, eventvolunteer.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, volunteer.VolunteerRecordsTable, volunteer.VolunteerRecordsColumn),
		)
		fromU = sqlgraph.SetNeighbors(vq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTrainings chains the current query on the "trainings" edge.
func (vq *VolunteerQuery) QueryTrainings() *TrainingQuery {
	query := (&TrainingClient{config: vq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := vq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := vq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(volunteer.Table, volunteer.FieldID, selector),
			sqlgraph.To(training.Table, training.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, volunteer.TrainingsTable, volunteer.TrainingsColumn),
		)
		fromU = sqlgraph.SetNeighbors(vq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Volunteer entity from the query.
// Returns a *NotFoundError when no Volunteer was found.
func (vq *VolunteerQuery) First(ctx context.Context) (*Volunteer, error) {
	nodes, err := vq.Limit(1).All(setContextOp(ctx, vq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{volunteer.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (vq *VolunteerQuery) FirstX(ctx context.Context) *Volunteer {
	node, err := vq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Volunteer ID from the query.
// Returns a *NotFoundError when no Volunteer ID was found.
func (vq *VolunteerQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = vq.Limit(1).IDs(setContextOp(ctx, vq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{volunteer.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (vq *VolunteerQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := vq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Volunteer entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Volunteer entity is found.
// Returns a *NotFoundError when no Volunteer entities are found.
func (vq *VolunteerQuery) Only(ctx context.Context) (*Volunteer, error) {
	nodes, err := vq.Limit(2).All(setContextOp(ctx, vq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{volunteer.Label}
	default:
		return nil, &NotSingularError{volunteer.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (vq *VolunteerQuery) OnlyX(ctx context.Context) *Volunteer {
	node, err := vq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Volunteer ID in the query.
// Returns a *NotSingularError when more than one Volunteer ID is found.
// Returns a *NotFoundError when no entities are found.
func (vq *VolunteerQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = vq.Limit(2).IDs(setContextOp(ctx, vq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{volunteer.Label}
	default:
		err = &NotSingularError{volunteer.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (vq *VolunteerQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := vq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Volunteers.
func (vq *VolunteerQuery) All(ctx context.Context) ([]*Volunteer, error) {
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryAll)
	if err := vq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Volunteer, *VolunteerQuery]()
	return withInterceptors[[]*Volunteer](ctx, vq, qr, vq.inters)
}

// AllX is like All, but panics if an error occurs.
func (vq *VolunteerQuery) AllX(ctx context.Context) []*Volunteer {
	nodes, err := vq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Volunteer IDs.
func (vq *VolunteerQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if vq.ctx.Unique == nil && vq.path != nil {
		vq.Unique(true)
	}
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryIDs)
	if err = vq.Select(volunteer.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (vq *VolunteerQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := vq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (vq *VolunteerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryCount)
	if err := vq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, vq, querierCount[*VolunteerQuery](), vq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (vq *VolunteerQuery) CountX(ctx context.Context) int {
	count, err := vq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (vq *VolunteerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryExist)
	switch _, err := vq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (vq *VolunteerQuery) ExistX(ctx context.Context) bool {
	exist, err := vq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the VolunteerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (vq *VolunteerQuery) Clone() *VolunteerQuery {
	if vq == nil {
		return nil
	}
	return &VolunteerQuery{
		config:               vq.config,
		ctx:                  vq.ctx.Clone(),
		order:                append([]volunteer.OrderOption{}, vq.order...),
		inters:               append([]Interceptor{}, vq.inters...),
		predicates:           append([]predicate.Volunteer{}, vq.predicates...),
		withVolunteerRecords: vq.withVolunteerRecords.Clone(),
		withTrainings:        vq.withTrainings.Clone(),
		// clone intermediate query.
		sql:  vq.sql.Clone(),
		path: vq.path,
	}
}

// WithVolunteerRecords tells the query-builder to eager-load the nodes that are connected to
// the "volunteer_records" edge. The optional arguments are used to configure the query builder of the edge.
func (vq *VolunteerQuery) WithVolunteerRecords(opts ...func(*EventVolunteerQuery)) *VolunteerQuery {
	query := (&EventVolunteerClient{config: vq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	vq.withVolunteerRecords = query
	return vq
}

// WithTrainings tells the query-builder to eager-load the nodes that are connected to
// the "trainings" edge. The optional arguments are used to configure the query builder of the edge.
func (vq *VolunteerQuery) WithTrainings(opts ...func(*TrainingQuery)) *VolunteerQuery {
	query := (&TrainingClient{config: vq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	vq.withTrainings = query
	return vq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Email string `json:"email,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Volunteer.Query().
//		GroupBy(volunteer.FieldEmail).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (vq *VolunteerQuery) GroupBy(field string, fields ...string) *VolunteerGroupBy {
	vq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &VolunteerGroupBy{build: vq}
	grbuild.flds = &vq.ctx.Fields
	grbuild.label = volunteer.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Email string `json:"email,omitempty"`
//	}
//
//	client.Volunteer.Query().
//		Select(volunteer.FieldEmail).
//		Scan(ctx, &v)
func (vq *VolunteerQuery) Select(fields ...string) *VolunteerSelect {
	vq.ctx.Fields = append(vq.ctx.Fields, fields...)
	sbuild := &VolunteerSelect{VolunteerQuery: vq}
	sbuild.label = volunteer.Label
	sbuild.flds, sbuild.scan = &vq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a VolunteerSelect configured with the given aggregations.
func (vq *VolunteerQuery) Aggregate(fns ...AggregateFunc) *VolunteerSelect {
	return vq.Select().Aggregate(fns...)
}

func (vq *VolunteerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range vq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, vq); err != nil {
				return err
			}
		}
	}
	for _, f := range vq.ctx.Fields {
		if !volunteer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if vq.path != nil {
		prev, err := vq.path(ctx)
		if err != nil {
			return err
		}
		vq.sql = prev
	}
	return nil
}

func (vq *VolunteerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Volunteer, error) {
	var (
		nodes       = []*Volunteer{}
		_spec       = vq.querySpec()
		loadedTypes = [2]bool{
			vq.withVolunteerRecords != nil,
			vq.withTrainings != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Volunteer).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Volunteer{config: vq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, vq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := vq.withVolunteerRecords; query != nil {
		if err := vq.loadVolunteerRecords(ctx, query, nodes,
			func(n *Volunteer) { n.Edges.VolunteerRecords = []*EventVolunteer{} },
			func(n *Volunteer, e *EventVolunteer) { n.Edges.VolunteerRecords = append(n.Edges.VolunteerRecords, e) }); err != nil {
			return nil, err
		}
	}
	if query := vq.withTrainings; query != nil {
		if err := vq.loadTrainings(ctx, query, nodes,
			func(n *Volunteer) { n.Edges.Trainings = []*Training{} },
			func(n *Volunteer, e *Training) { n.Edges.Trainings = append(n.Edges.Trainings, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (vq *VolunteerQuery) loadVolunteerRecords(ctx context.Context, query *EventVolunteerQuery, nodes []*Volunteer, init func(*Volunteer), assign func(*Volunteer, *EventVolunteer)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Volunteer)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.EventVolunteer(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(volunteer.VolunteerRecordsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.event_volunteer_volunteer
		if fk == nil {
			return fmt.Errorf(`foreign-key "event_volunteer_volunteer" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "event_volunteer_volunteer" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (vq *VolunteerQuery) loadTrainings(ctx context.Context, query *TrainingQuery, nodes []*Volunteer, init func(*Volunteer), assign func(*Volunteer, *Training)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Volunteer)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Training(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(volunteer.TrainingsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.training_volunteer
		if fk == nil {
			return fmt.Errorf(`foreign-key "training_volunteer" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "training_volunteer" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (vq *VolunteerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := vq.querySpec()
	_spec.Node.Columns = vq.ctx.Fields
	if len(vq.ctx.Fields) > 0 {
		_spec.Unique = vq.ctx.Unique != nil && *vq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, vq.driver, _spec)
}

func (vq *VolunteerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(volunteer.Table, volunteer.Columns, sqlgraph.NewFieldSpec(volunteer.FieldID, field.TypeUUID))
	_spec.From = vq.sql
	if unique := vq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if vq.path != nil {
		_spec.Unique = true
	}
	if fields := vq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, volunteer.FieldID)
		for i := range fields {
			if fields[i] != volunteer.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := vq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := vq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := vq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := vq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (vq *VolunteerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(vq.driver.Dialect())
	t1 := builder.Table(volunteer.Table)
	columns := vq.ctx.Fields
	if len(columns) == 0 {
		columns = volunteer.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if vq.sql != nil {
		selector = vq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if vq.ctx.Unique != nil && *vq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range vq.predicates {
		p(selector)
	}
	for _, p := range vq.order {
		p(selector)
	}
	if offset := vq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := vq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// VolunteerGroupBy is the group-by builder for Volunteer entities.
type VolunteerGroupBy struct {
	selector
	build *VolunteerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (vgb *VolunteerGroupBy) Aggregate(fns ...AggregateFunc) *VolunteerGroupBy {
	vgb.fns = append(vgb.fns, fns...)
	return vgb
}

// Scan applies the selector query and scans the result into the given value.
func (vgb *VolunteerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vgb.build.ctx, ent.OpQueryGroupBy)
	if err := vgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VolunteerQuery, *VolunteerGroupBy](ctx, vgb.build, vgb, vgb.build.inters, v)
}

func (vgb *VolunteerGroupBy) sqlScan(ctx context.Context, root *VolunteerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(vgb.fns))
	for _, fn := range vgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*vgb.flds)+len(vgb.fns))
		for _, f := range *vgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*vgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// VolunteerSelect is the builder for selecting fields of Volunteer entities.
type VolunteerSelect struct {
	*VolunteerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (vs *VolunteerSelect) Aggregate(fns ...AggregateFunc) *VolunteerSelect {
	vs.fns = append(vs.fns, fns...)
	return vs
}

// Scan applies the selector query and scans the result into the given value.
func (vs *VolunteerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vs.ctx, ent.OpQuerySelect)
	if err := vs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VolunteerQuery, *VolunteerSelect](ctx, vs.VolunteerQuery, vs, vs.inters, v)
}

func (vs *VolunteerSelect) sqlScan(ctx context.Context, root *VolunteerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(vs.fns))
	for _, fn := range vs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*vs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
